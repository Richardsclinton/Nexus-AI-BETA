/**
 * Force x402 version 1 for the resource server.
 * The facilitator (https://facilitator.x402.rs) only supports x402Version 1.
 * @x402/core defaults to 2; we alias this module so the server uses 1.
 */
export const x402Version = 1;
